# Illustrated IF Studio C++ stub

Header-only sketch for condition parity: `include/if/conditions.hpp`.

## Next

1. Add `Loader` reading `project.json` + scenes with nlohmann/json
2. Headless CLI matching `parity-test.mjs` fixtures
3. raylib sectioned UI
4. CMake export template invoked by `scripts/export-cpp.mjs`

See Obsidian: [[05 Export/C++ Export Pipeline]]
